$kind: http-request
description: ""
url: "{{django}}v1/databases/{{databaseId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Retrieve databases", () => {
          pm.expect(pm.response.code).to.eql(200)
      })
    language: text/javascript
order: 3170589602753539
