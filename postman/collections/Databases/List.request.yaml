$kind: http-request
description: ""
url: "{{django}}v1/databases/"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Retrieve databases", () => {
          pm.expect(pm.response.code).to.eql(200)

          const database = pm.response.json()[0]
          pm.expect(database).to.haveOwnProperty("id")
          pm.environment.set("databaseId", database.id)
      })
    language: text/javascript
order: 1789410014653
