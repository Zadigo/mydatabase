$kind: http-request
url: "{{django}}v1/tables/create"
method: POST
body:
  type: json
  content: |-
    {
        "database": "{{databaseId}}",
        "name": "Some simple table",
        "description": "Some ease table description",
        "active_document_datasource": null,
        "component": "data-table",
        "slug": null,
        "active": true
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("create table", () => {
          pm.expect(pm.response.status).to.be(200)
          pm.expect(pm.response.json().id).to.greaterThan(0)
      })
    language: text/javascript
order: 1789410617442
