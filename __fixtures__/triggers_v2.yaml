on:
  event:
    name: insert | delete | update
    database: database_name
    timing: before | after
    columns:
      - firsname
      - lastname

actions:
  some-action-name:
    steps:
      - function: count | sum | etc.
        orientation: column | row
        default: null
        aggregate: false

      - function: lower
        orientation: column
        defualt: null
        aggegrate: false
